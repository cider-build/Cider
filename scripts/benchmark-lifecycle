#!/bin/sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
python="$script_dir/../node/.venv/bin/python"
if [ ! -x "$python" ]; then
  echo "node/.venv is missing; run 'cd node && uv sync'" >&2
  exit 1
fi
exec "$python" "$script_dir/benchmarks/lifecycle.py" "$@"
