---
title: Create a server
full: true
_openapi:
  preload:
    - ./public/api-reference/openapi.yaml
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >
          Creates a named server. Returns at once with `status: "provisioning"`.
          Cider clones the

          base image at the requested size on a connected node, installs the
          requested software,

          applies `env`, runs `setup`, starts `start`, and moves the server to
          `running`.

          Poll `GET /servers/{server_id}` for progress.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="./public/api-reference/openapi.yaml" operations={[{"path":"/servers","method":"post"}]} showDescription />
    </>
  );
}